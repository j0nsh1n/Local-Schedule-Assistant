# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── PyInstaller build output ────────────────────────────
build/
dist_exe/
*.spec

# ── Abandoned Tauri prototype (kept locally, not published) ──
node_modules/
dist/
src-tauri/
package.json
package-lock.json
set-ollama-env.bat

# ── Local app data / secrets (normally in ~/.daily-scheduler) ──
credentials.json
token.json
activities.json
settings.json

# ── Logs / scratch ──────────────────────────────────────
*.log
_*.txt
